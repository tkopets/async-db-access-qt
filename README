This is a sample application demonstrating asynchronous database
access with Qt. To compile:

 qmake
 make (or nmake)

By default, this sample app will use sqlite to create a local
database. See db.h to change this behavior.




